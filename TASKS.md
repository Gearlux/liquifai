# liquifai — backlog

Open work for this project. Cross-cutting / multi-project initiatives live in the
workspace root `TASKS.md`. Completed items are not archived here — git history is the record.

- [ ] Declare `liquifai.apps` entry point in sairen's pyproject (work machine — submodule not checked out here) so its discovery stops depending on the probe fallback @todo @chore
- [ ] Escalate dropped-override-token warning to a hard error after a bake period @todo @ux  <!-- the `--` pass-through escape hatch shipped 2026-07-26 (walk.tokenize), so the prerequisite is met. -->
- [ ] Pluggable command registration via entry points @feature  <!-- Liquifai CLI verbs — separate from the confluid.configurables entry-point group; see Navigaitor MCP Surface for the @configurable discovery side. -->

- [ ] **Migrate off the deprecated confluid YAML tag spelling — DEADLINE: confluid 0.4.0 REMOVES it** @todo @docs — every example, doc and docstring still teaches `!class:` / `!lazy:` / `!ref:`, so running the documented path prints a `FutureWarning` telling the reader their config is obsolete (reproduce: `python examples/liquifai_app.py`). Census 2026-08-13 — **31 files, 102 lines**: 3 example YAMLs (`examples/demo_config.yaml`, `examples/broadcast_demo.yaml`, `examples/linefit/linefit.yaml`), 3 example `.py` (inline YAML + prose), 3 `docs/*.md` (incl. `docs/architecture.md` §4's own quoted configs), `README.md:39`, 7 library docstrings (`di.py`, `overrides.py`, `core.py`), and 14 test files (61 lines). `confluid-migrate <path>` rewrites the YAML half only — the prose, docstrings and tests are hand work, and confluid learned the hard way that migrating configs while leaving comments describing the old spelling is worse than not migrating (`confluid/tests/test_canonical_spelling.py`). **Two traps:** (1) `examples/linefit/run.py:47` asserts `"!lazy:GradientDescent" in out` — a CI self-test pinning the tag string in `print-config` output, so it must be updated in the same change or CI goes red; (2) liquifai has no canonical-spelling scan, so nothing stops the tag reappearing — port confluid's when this lands.
- [ ] **Remove the 9 deprecated `liquifai.core` aliases (v1.0).** They warn on access now and nothing inside liquifai uses them. BLOCKED here: the external consumers (streamstudio / recordstream) are bare submodule shells on this machine (0 `.py` files, mid-rename) — audit + migrate them on the work machine, then delete `core._DEPRECATED_ALIASES` and `core.__getattr__` and drop `tests/test_deprecated_aliases.py` @todo @cleanup
- [ ] Revisit `liquifai.bridge` when a SECOND consumer appears: stabilise the spec vocabulary against both and fold it into the version contract, or split it into its own distribution (docs/architecture.md §6) @todo @architecture
- [ ] DI silently ignores wrapped annotations: `model: Optional[Model] = None` resolves to `None` instead of an injected instance, with no warning (`di.resolve_kwargs` only matches a bare `@configurable` class). Either handle `Optional`/`Union` — confluid's `to_pydantic` already has the configurable-union rule — or warn when an unresolvable annotation CONTAINS a configurable @todo @di
- [ ] **Drop the bare-key `Fluid.kwargs` write ENTIRELY — MEASURED SAFE, deferred to 0.2.0** @medium @architecture — the flat branch is redundant: `apply_overrides` already merges a bare `--key` into the document, so confluid's broadcasting delivers it at materialize time to every declaring node, and liquifai's write into `Fluid.kwargs` re-implements a delivery it claims to defer. **The prerequisite is DONE** — `tests/test_override_delivery_matrix.py` (14 shapes: top-level, nested-in-kwargs, listed, `!ref:`-shared, document-level `!lazy:` flowed later, `__init__`-body lazy slot, deferred `!class:` stub, both broadcast opt-outs, `**kwargs`, addressed `--name.key`, unresolvable target ×2, own-inline-kwarg precedence) **passes IDENTICALLY with and without the write**, and under the experiment recordstream (703) + sonair (568) stayed green and a real 5-cell CLI sweep produced equivalent metrics. Only 8 tests moved, all in `test_override_broadcast.py`, all asserting `fluid.kwargs` directly — i.e. the mechanism, not a delivered outcome. **Deferred anyway, for two reasons.** (1) *Release shape*: 0.1.1 is a patch whose changelog says "no API changes", and this silently changes which keys reach which objects — a dropped override is indistinguishable from a config that was never overridden. It belongs in 0.2.0 with its own bake, not bundled behind a bug fix. (2) *The area is treacherous*: the FIRST version of the matrix gave a false PASS because its harness applied overrides to raw YAML and loaded afterwards, while `core.py` does the reverse — `confluid.load(data, flow=False)` at :672, `_apply_overrides` at :701. Anything re-verifying this MUST keep `config_after_cli`'s ordering. When it lands: delete the flat write, rewrite those 8 tests against delivered outcomes rather than `fluid.kwargs`, and revisit AGENTS.md + `docs/architecture.md` record 4 — with the flat branch gone, liquifai stops calling `accepts_broadcast` at all and only the addressed form remains. **Re-verify the matrix against `_move_cli_keys_last` (2026-08-04):** with the write gone, EVERY bare override is delivered by cascade, so document position becomes the only thing making a CLI value win — the repositioning stops being a fix for the `**kwargs` subset and becomes load-bearing for all of them.
- [ ] **The param-name and flat-fallback DI blocks are still hoisted into the marker's kwargs** @low @di — the class-name branch stopped hoisting in 0.1.1 (a hoisted block has no document position, so it could never lose a precedence contest — `docs/architecture.md` §4). The other two selections have the same latent shape but no fix available in isolation: `widget: {size: 7}` matches by class name or instance `name:`, and a synthesized marker has neither, so leaving it in the document would make it unreachable rather than merely mis-ordered. Giving the marker a `name:` would make it addressable — worth doing together with the `Fluid.kwargs` write removal below, not before. @todo
- [ ] **Consider teaching the dotted `--head.key` form to walk ATTRIBUTE paths** @low @feature — it addresses an instance by its YAML `name:`; `--optimizer.lr` aimed at an `__init__`-body slot addresses nothing. That is no longer SILENT (2026-08-03 it warns and names the working spelling), so this is now an ergonomics question rather than a correctness one. Note the engine-side grammar already exists — confluid resolves `optimizer.lr:` written INSIDE a marker's block — so the work is mapping the CLI form onto it rather than inventing a path syntax. Weigh against the bare form, which reaches the same slot today.
- [ ] `flags.parse_globals` and `flags.bind_dimension_flags` are two sequential passes over the same token list; they are genuinely ordered (the second needs the config the first located) but could share one option-token splitter with `overrides.parse_override_args` @low @cleanup
- [ ] **Rich integration:** expand the use of Rich for experiment progress bars. @low @feature
