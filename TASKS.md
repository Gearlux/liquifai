# liquifai — backlog

Open work for this project. Cross-cutting / multi-project initiatives live in the
workspace root `TASKS.md`. Completed items are not archived here — git history is the record.

- [ ] Declare `liquifai.apps` entry point in sairen's pyproject (work machine — submodule not checked out here) so its discovery stops depending on the probe fallback @todo @chore
- [ ] **POSTPONED — architecture-review follow-ups (2026-08-21). BLOCKED until sairen is in the loop (user decision 2026-08-21): none of the items below start before sairen's checkout is available to audit and verify against — including the ones that look workspace-only.** The review's frame: liquifai serves two consumer populations with almost disjoint surfaces — the 11 workspace apps use `LiquifyApp(name, description)` + `@script_command` (36 registrations, 15 via the matrainer factory `matrainer/cli.py:159`) + `run()` + `liquifai.context`, with `positionals=` used exactly once (chainwind `cli.py:94`); sairen (off-machine) is the sole consumer of the bridge, `@operation`/`build_commands`, presenters, both context factories, `make_mcp_tools`, and the dynamic-completion machinery. The bridge STAYS (user decision 2026-08-21); the risk to manage is that its half of the library is developed blind against hand-rolled fakes. @blocked @architecture
    - **Bridge hardening** (defects verified by execution): (a) unknown `@expose(**options)` keys are swallowed silently — `@expose(verb='info', colums=…, presentaton='fields')` produces a working-but-wrong op (`spec.py:107`/`:171`; `_validate_spec` at `group.py:247-255` gates adapter keys only) — should raise `CommandDefinitionError` at decoration like adapter keys; (b) falsy values break both param paths in `resolve_call` (`policies.py:131-140`): required `P("count")` with `count=0` raises "Missing required argument(s)", optional explicit `--limit 0` is silently dropped (`omit_empty=True` default, `spec.py:59`) — switch to `is None`/sentinel semantics; (c) required params always annotated `str` (`policies.py:115`), degrading the MCP JSON schema; (d) `_synthesize` re-implements `graft_signature`'s annotations block (`group.py:80-84` vs `introspection.py:43-47`) — the one violation of the signature-surgery single-source mandate; (e) `BridgeConnection` is `@runtime_checkable` but never checked, `Group.sub` written never read, `CallPolicy.build`/`ItemsPolicy.build` share ~12 identical lines. Plus one structural tripwire so regressions surface: sairen CI installing `liquifai @ git+…@main` and/or growing `tests/bridge_fakes.py` toward a contract suite (group tests hand-build `PolicyContext` instead of going through `SdkBridge._context`, so the two can drift).
    - **Completion engine** (bugs verified by execution): `complete_from_tree` re-counts positionals from `parsed[walk.args_index:]` (`engine.py::_command_candidates`; the string search for the command token is gone since 2026-08-22 — `Walk.args_index`) instead of using `walk.positional_values` already in scope — a promoted config token is still counted as a positional (`app train demo.yaml <TAB>` hints `<beta>` when `<alpha>` is unfilled); a flag-supplied prior positional breaks the dependent lookup (`engine.py:281` counts it filled, `:296-298` indexes positional tokens only — `--name ds1` yields `<version>` placeholder where `ds1` yields real values); `used_keys` restates the override grammar (`engine.py:196-213` vs `overrides.py:59-121`, `~key` already diverged — a stdlib-only `grammar.override_keys()` would serve both); `--config`/`-c` hardcoded at `engine.py:163` instead of derived from `GLOBAL_FLAG_SPECS`; the `.yaml`-suffix rule exists twice (`engine.py:76`, `router.py:60`). No test covers script-command-with-positionals completion — add with the fix.
    - **Cache robustness**: a cache file whose JSON is not a dict (`null`, a list) raises uncaught `AttributeError` through all six readers (verified) and `_fast_complete.main()` has no top-level guard — the never-break-the-shell contract lives only in the templates' `2>/dev/null`; the three age/`changed_at` readers skip the version check the value readers perform (`cache.py:120`/`:195`/`:208` vs `:106`/`:181`), so after a version bump the fresh-looking age suppresses the self-heal; unify the six readers/three writers into one guarded `_read_payload`/`_write_payload` pair (retires both); writes are non-atomic, `.pending` throttle markers never unlinked, per-input caches never evicted; an explicit `--refresh-completions` whose providers ALL crash prints "No positional completion providers registered" (`tree.py:248-276` swallow → `completion_cli.py:106`) — factually wrong, should report the failures.
    - **Deletions**: the subprocess probe (~87 lines: `discover.py:30-89`, `:143-169`, incl. the hand-maintained ML-binary denylist) + the `--show-completion` cache side effect it forces (`completion_cli.py:53-61`) — prerequisites are the sairen entry-point item above PLUS matrainer declaring `liquifai.apps` (it ships a liquifai app, `[project.scripts]` only); the dead `_HELPERS_MARKER`/`_HELPERS_END_MARKER` duplicate pair at `shells.py:369-370` (live pair is install.py's); `_introspect_function_keys` (`tree.py:401-408`, only its own tests call it); the 13 `completion/__init__` re-exports nothing outside the package reaches (external usage of the 50-name surface is exactly `detect_shell` + `install_for_apps`, both aisland); the 9 deprecated core aliases — the 2026-08-21 workspace audit found ZERO consumers (every external match is prose in comments/AGENTS files; streamstudio/recordstream are populated here and clean), so only a grep of sairen remains before deleting (updates the standing alias item above).
    - **Contract questions for 0.2.0** (decide alongside the `Fluid.kwargs`-write removal below, since all touch the delivery contract): positional values bind verbatim strings while the `--flag` spelling of the same parameter goes through `parse_value` — `app download 7` gives `"7"`, `--count 7` gives `7` (`core.py:229-236` documents it; `di.resolve_kwargs` has the annotation at `di.py:44` and could coerce); `run()` takes no argv (tests monkeypatch `sys.argv`; the embedding-host story behind the raise-don't-exit mandate is incomplete without `run(argv=None)`); no public read-only accessor for registered commands (five downstream suites read `app._commands`/`_script_cmds`/`__liquifai_flow_mode__`); `expand_strings`' blanket `expandvars` has no escape for a literal `$` in a YAML value and is config-engine work living in the CLI layer (confluid itself never expands `$VAR`, so non-liquifai confluid consumers silently lack the substitution).
    - **Cross-project hygiene found in passing** (park here with the rest): waivefront `pyproject.toml:8` and streamstudio declare a liquifai dependency and use nothing; aisland imports `liquifai.completion` (`completion.py:196`) without declaring it; sonair and raidar-ultralytics declare the `liquifai.apps` entry point while depending on liquifai only transitively; chainwind/purifai/traidwind mark liquifai `ignore_missing_imports` in mypy despite `py.typed`; `LiquifyContext.logger: Any` should be the named loggair type.
- [ ] **Host-facts filter is exact only for positively-declared dimensions** @low @host — `host.auto_scopes` reads `confluid.discover_dimension_values`, which reports POSITIVE declarations only, so a document carrying both `!scope:os=linux` and `!notscope:os=darwin` drops the auto value on a Mac and leaves the negation active (confluid itself would have accepted `os=darwin` there and dropped the block). Documented as a known limit in `docs/host-facts.md`. The exact fix needs confluid to expose which dimensions carry a negation — a small accessor over its ONE dimension walker (`confluid/scopes.py::_walk_dimensions` already computes it) — which costs a confluid release plus a floor bump here; deliberately not taken while no config in the workspace has that shape.
- [ ] Escalate dropped-override-token warning to a hard error after a bake period @todo @ux  <!-- the `--` pass-through escape hatch shipped 2026-07-26 (walk.tokenize), so the prerequisite is met. -->
- [ ] Pluggable command registration via entry points @feature  <!-- Liquifai CLI verbs — separate from the confluid.configurables entry-point group; see Navigaitor MCP Surface for the @configurable discovery side. -->

- [x] ~~**Migrate off the deprecated confluid YAML tag spelling**~~ — **CLOSED 2026-08-15, nothing to do:** confluid reversed the deprecation (its architecture record 19 — tags are the preferred AUTHORING form, the reserved keys are what `hydraide` EMITS; neither warns; `confluid-migrate` and confluid's canonical-spelling scan are deleted). liquifai's tag-spelled examples, docs and the `examples/linefit/run.py:47` self-test are correct as they stand.
- [ ] **Remove the 9 deprecated `liquifai.core` aliases (v1.0).** They warn on access now and nothing inside liquifai uses them. BLOCKED here: the external consumers (streamstudio / recordstream) are bare submodule shells on this machine (0 `.py` files, mid-rename) — audit + migrate them on the work machine, then delete `core._DEPRECATED_ALIASES` and `core.__getattr__` and drop `tests/test_deprecated_aliases.py` @todo @cleanup
- [ ] Revisit `liquifai.bridge` when a SECOND consumer appears: stabilise the spec vocabulary against both and fold it into the version contract, or split it into its own distribution (docs/architecture.md §6) @todo @architecture
- [ ] DI silently ignores wrapped annotations: `model: Optional[Model] = None` resolves to `None` instead of an injected instance, with no warning (`di.resolve_kwargs` only matches a bare `@configurable` class). Either handle `Optional`/`Union` — confluid's `to_pydantic` already has the configurable-union rule — or warn when an unresolvable annotation CONTAINS a configurable @todo @di
- [ ] **Drop the bare-key `Fluid.kwargs` write ENTIRELY — MEASURED SAFE, deferred to 0.2.0** @medium @architecture — the flat branch is redundant: `apply_overrides` already merges a bare `--key` into the document, so confluid's broadcasting delivers it at materialize time to every declaring node, and liquifai's write into `Fluid.kwargs` re-implements a delivery it claims to defer. **The prerequisite is DONE** — `tests/test_override_delivery_matrix.py` (14 shapes: top-level, nested-in-kwargs, listed, `!ref:`-shared, document-level `!lazy:` flowed later, `__init__`-body lazy slot, deferred `!class:` stub, both broadcast opt-outs, `**kwargs`, addressed `--name.key`, unresolvable target ×2, own-inline-kwarg precedence) **passes IDENTICALLY with and without the write**, and under the experiment recordstream (703) + sonair (568) stayed green and a real 5-cell CLI sweep produced equivalent metrics. Only 8 tests moved, all in `test_override_broadcast.py`, all asserting `fluid.kwargs` directly — i.e. the mechanism, not a delivered outcome. **Deferred anyway, for two reasons.** (1) *Release shape*: 0.1.1 is a patch whose changelog says "no API changes", and this silently changes which keys reach which objects — a dropped override is indistinguishable from a config that was never overridden. It belongs in 0.2.0 with its own bake, not bundled behind a bug fix. (2) *The area is treacherous*: the FIRST version of the matrix gave a false PASS because its harness applied overrides to raw YAML and loaded afterwards, while `core.py` does the reverse — `confluid.load(data, flow=False)` at :672, `_apply_overrides` at :701. Anything re-verifying this MUST keep `config_after_cli`'s ordering. When it lands: delete the flat write, rewrite those 8 tests against delivered outcomes rather than `fluid.kwargs`, and revisit AGENTS.md + `docs/architecture.md` record 4 — with the flat branch gone, liquifai stops calling `accepts_broadcast` at all and only the addressed form remains. **Re-verify the matrix against `_move_cli_keys_last` (2026-08-04):** with the write gone, EVERY bare override is delivered by cascade, so document position becomes the only thing making a CLI value win — the repositioning stops being a fix for the `**kwargs` subset and becomes load-bearing for all of them.
- [ ] **The param-name and flat-fallback DI blocks are still hoisted into the marker's kwargs** @low @di — the class-name branch stopped hoisting in 0.1.1 (a hoisted block has no document position, so it could never lose a precedence contest — `docs/architecture.md` §4). The other two selections have the same latent shape but no fix available in isolation: `widget: {size: 7}` matches by class name or instance `name:`, and a synthesized marker has neither, so leaving it in the document would make it unreachable rather than merely mis-ordered. Giving the marker a `name:` would make it addressable — worth doing together with the `Fluid.kwargs` write removal below, not before. @todo
- [ ] **Consider teaching the dotted `--head.key` form to walk ATTRIBUTE paths** @low @feature — it addresses an instance by its YAML `name:`; `--optimizer.lr` aimed at an `__init__`-body slot addresses nothing. That is no longer SILENT (2026-08-03 it warns and names the working spelling), so this is now an ergonomics question rather than a correctness one. Note the engine-side grammar already exists — confluid resolves `optimizer.lr:` written INSIDE a marker's block — so the work is mapping the CLI form onto it rather than inventing a path syntax. Weigh against the bare form, which reaches the same slot today.
- [ ] `flags.parse_globals` and `flags.bind_dimension_flags` are two sequential passes over the same token list; they are genuinely ordered (the second needs the config the first located) but could share one option-token splitter with `overrides.parse_override_args` @low @cleanup
- [ ] **Rich integration:** expand the use of Rich for experiment progress bars. @low @feature
